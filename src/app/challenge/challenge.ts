export class Challenge {
  constructor(id: number,
    created: string,
    group: number,
    address: string,
    guesses: Array<any>,
    askingPrice: number,
    soldForPrice: number,
    dateSold: string) {
  }
}
